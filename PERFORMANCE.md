# Производительность: что измерено

Замеры на устройстве, к которому привязан проект: **Redmi Note 8T**,
Snapdragon 665, Adreno 610, экран 1080×2340 при 60 Гц, Android 11.
То же железо, на котором делались замеры SWARM, поэтому цифры отсюда
и из `SwarmSurvivorExpirience/VISUAL_OPTIMIZATION.md` сравнимы напрямую.

Целевой кадр — **16.7 мс**.

Здесь только то, что измерено на устройстве. Ни одна цифра не получена
в редакторе: он добавляет своих накладных расходов и в этом проекте
регулярно показывал совсем другую картину.

---

## 1. Главная находка: земля стоила две трети кадра

Игра шла на 37 кадрах. Виновником оказался **один квад земли** с материалом
на `Universal Render Pipeline/Lit`.

| земля на шейдере | кадр | fps |
|---|---|---|
| URP/Lit | 27.3 мс | 37 |
| URP/Simple Lit | 16.8 мс | 60 |
| URP/Unlit | 16.7 мс | 60 |
| **свой `WarfareSurvivor/CheapGround`** | **16.7 мс** | **60** |
| земли нет вовсе | 16.7 мс | 60 |

Земля на дешёвом шейдере стоит **ровно столько же, сколько её отсутствие**.
Разница с Lit — **10.5 мс**, две трети бюджета кадра.

**Почему так дорого.** Земля занимает весь экран, а `Lit` считает на каждом
пикселе полный PBR: металличность, шероховатость, отражения, сферические
гармоники, до четырёх дополнительных источников. Ни одно из этого земле
не нужно.

**Свой шейдер** (`Assets/_Project/Art/Shaders/CheapGround.shader`) делает
только необходимое: текстура, главный свет, его тень, непрямой свет одной
константой. Нормаль не интерполируется — земля плоская. Прохода отбрасывания
тени нет — земля тень не отбрасывает.

Unlit не подошёл бы: он не принимает тени, а тени на земле — единственное,
что сажает персонажей в сцену.

---

## 2. Что оказалось НЕ виновато

Перебирали по одному, отключая маской камеры (рисование гаснет, логика
продолжает считаться).

| подозреваемый | вердикт |
|---|---|
| толпа зомби | 25 штук ≈ 6 мс, но всё вместе укладывалось в 60 fps |
| отряд 25 бойцов | ≈ 5 мс |
| руины, 14 штук с осыпью | **0.6 мс** — геометрия здесь дёшева |
| тени | в чистом сравнении ≈ 0 |
| HDR-буфер | 28.3 мс с ним против 28.4 без — в пределах шума |
| интерфейс | не влияет |
| расталкивание зомби, поиск целей, логика отряда | **ноль**: выключение всех трёх не сдвинуло кадр |

Отдельно стоит запомнить: **вся наша игровая логика вместе не стоит ничего
заметного**. Двести зомби с расталкиванием через пространственную сетку,
поиск целей по расписанию, строй, отброс — всё это не видно на фоне одного
неудачно настроенного материала.

---

## 3. Масштаб рендера: считать по GPU, а не по кадрам

После починки земли все настройки упираются в развёртку экрана и читаются
как ровно 16.7 мс. Сравнивать под потолком нечего — выбирать надо
по времени GPU.

| настройка | GPU |
|---|---|
| масштаб 0.8 | 13.6–14.0 мс |
| масштаб 1.0 | 14.5–15.3 мс |
| тени 70 м против 60 м | разницы нет |

**Выбран масштаб 1.0.** Уменьшение до 0.8 убирает 36% пикселей, а выигрывает
1.2 мс из пятнадцати — восемь процентов. Остальное от разрешения не зависит:
теневая карта, вершины, вызовы отрисовки. Плюс при масштабе меньше единицы
URP добавляет блит растяжения, который часть выигрыша съедает обратно.

**Побочный эффект, который видно глазами.** При масштабе меньше единицы
картинка пересэмплируется каждый кадр, и мелкие детали на движении дрожат.
Кадры при этом одинаковые, но движение читается как менее плавное.

**Дальность теней сокращена до 60 м** бесплатно: видимая земля кончается
на 56 м, лишние метры только размазывали ту же карту 1024 по большей площади.

---

## 4. Как мерить, чтобы не соврать

Инструмент замера в этом проекте **дважды врал**, и оба раза это уводило
расследование в сторону на несколько итераций. Правила ниже — цена этих
ошибок.

**Худший кадр не может быть меньше среднего.** Если так вышло — врёт замер,
а не железо. У нас среднее оказалось завышено на 26%: кадры прогрева
пропускались вычитанием из счётчика, а сумма времени копилась с первого
кадра. Родился «пол в 20.8 мс», которого не существовало в природе.
Теперь строка замера сама себя проверяет и приписывает предупреждение.

**Прогрев считать кадрами, а не секундами.** Кадр загрузки сцены длится
десятки секунд и сам проскакивает любое окно, заданное во времени.

**Ограничитель кадров снимать на время замера.** Под ним всё, что быстрее
16.7 мс, читается как ровно 16.7. Сравнивали две настройки и обе показывали
потолок — мерили ограничитель, а не затраты.

**Держать нагрузку постоянной.** За минуту прогона толпа успевала вырасти
с 20 до 190, а каждый зомби стоит заметных долей миллисекунды. Поздние
ступени сравнивать было не с чем.

**Гасить рисование, а не компоненты.** Маска камеры убирает отрисовку,
оставляя счёт. Выключив компонент, померишь заодно и отсутствие его работы.

**Не сравнивать ступени с разной нагрузкой.** На этом я один раз объявил
тени виновником, стоящим 18 мс: сравнил ступень с 30 зомби и ступень с 71.
В честном сравнении тени стоили ноль.

**Проверять, что закрепление нагрузки не затёрто.** Стенд закреплял толпу
в `Awake`, а строкой ниже безусловно ставил её обратно в 30. Сравнения
шейдеров зомби полгода шли не на полутора сотнях, а на тридцати, и цифра
«свой тун экономит 4 мс» относится к втрое меньшей толпе, чем считалось.

**Разовую работу делать до начала счёта.** Ступень с застывшей позой
создаёт каждому зомби меш-фильтр и рисователь. В первом круге эта разовая
работа легла прямо в замер: худший кадр доходил до 66 мс, среднее выходило
на семь миллисекунд выше настоящего, и вывод получился ОБРАТНЫЙ верному —
будто кости самая дешёвая из статей. Признак был на виду: числа одной
и той же ступени ехали вниз от круга к кругу. **Пока значения ползут,
измерен прогрев, а не нагрузка.**

**Первые круги любого сравнения считать прогревочными.** Компиляция
вариантов шейдера, создание материалов и мешей — всё это одноразовое
и всё это дорого.

---

## 5. Инструменты

**Счётчик кадра** — левый верхний угол, всегда в сцене. Показывает fps,
среднее, **худший кадр** в окне и за сеанс, разделение CPU/GPU, число зомби
и бойцов и **площадь перерисовки трасс в квадратных метрах**. Последнее —
прямо по пункту «что бы я сделал иначе» из документа SWARM.

Выключается флагом `showFrameMeter` в `ArenaConfig`.

**Стенд нагрузки** (`PerformanceSweep`) — в сцену не кладётся, пока не нужен.
Включается флагом `debugSweep` и пересборкой сцены пунктом меню
**WarfareSurvivor → Build Test Arena**. Режимы:

- `Full` — один проход по всем ступеням, ищем виновника просадки;
- `Ground` — сравнение шейдеров земли по кругу;
- `Pipeline` — сравнение масштаба рендера и дальности теней по кругу;
- `Ramp` — растущая толпа: ищем численность, на которой кадр ломается;
- `Zombies` — шейдер и отбрасывание тени на закреплённой толпе;
- `Skinning` — за что платим на зомби: кости, аниматор, пиксели, вызовы.

В режимах с закреплённой толпой ступень не начинает считать, пока толпа
не набрана. Порог — две трети потолка: потолок спавнера считает и трупы,
доигрывающие смерть, а счётчик живых до него не дотягивается никогда.

Сравнения крутятся без конца с подписью на экране: на них смотрят глазами,
а не читают лог постфактум. Стенд возвращает все тронутые настройки обратно —
масштаб рендера, дальность теней, шейдер земли живут в ассетах и иначе
пережили бы выход из игры.

**Чтение с устройства.** Сборка отправляется по вайфаю, лог снимается
`adb logcat -s Unity:V`. Строки замера помечены `[Кадр]` и `[Стенд]`.

---

## 6. Цена толпы: за что платим на зомби

Толпа упирается в GPU линейно. Вопрос был, из чего складывается цена одного
зомби, и ответ решал судьбу большой переделки — запекания анимации
в текстуру и перевода толпы на `BatchRendererGroup`.

**Геометрия сразу вне подозрений:** у зомби 1107 треугольников. Сотня таких —
110 тысяч, для Adreno 610 пустяк. Значит платим за то, что каждый зомби
отдельный объект.

Стенд снимает по одному подозреваемому за ступень. Застывшая поза — тот же
меш, та же площадь на экране, то же число вызовов; убраны ровно кости.
Замеры на сотне зомби, тёплые круги, по GPU:

| ступень | GPU |
|---|---|
| как есть (кости + аниматор) | 23–26 мс |
| застывшая поза | 14–16 мс |
| + аниматор выключен | 14.0–14.5 мс |
| + плоский шейдер (URP/Unlit) | 14.0–14.8 мс |
| сотня вызовов, пакетчик SRP выключен | 14.0 мс |
| один материал и инстансинг | 13.7 мс |

**Разбор цены сотни зомби:**

| статья | цена |
|---|---|
| **кости (скиннинг)** | **около 10 мс** |
| аниматор | 0.5 мс |
| материал TCP2 против плоского | в пределах шума |
| **вызовы отрисовки** | **0.3 мс** |

### Что из этого следует про BatchRendererGroup

`BatchRendererGroup` рисует много одинаковых мешей одним вызовом, забирая
данные экземпляров из буфера. Он убирает **вызовы отрисовки** — а они у нас
стоят 0.3 мс на сотню. **Собственный вклад BRG для нас в пределах
погрешности.**

Проверено прямо: последние две ступени рисуют одну и ту же картинку сотней
вызовов и единицами. Пакетчик SRP в обеих выключен — он вызовы не убирает,
а в URP имеет приоритет над инстансингом, и с ним сравнение показало бы
ноль на пустом месте.

Весь выигрыш на толпе лежит в костях, и убирает их **не BRG, а запекание
анимации в текстуру**: кадры анимации сохраняются в текстуру как позиции
вершин, вершинный шейдер читает нужный по времени, скиннинга не остаётся
вовсе. BRG к этому прилагается бесплатно, но сам по себе не выигрывает
ничего.

**Чего это стоит.** Инструмент запекания клипов, шейдер, читающий текстуру
в вершинной стадии, плюс его проход отбрасывания тени, и переделка зомби
из объектов в данные. Взамен — около десяти миллисекунд на сотне, то есть
примерно вдвое больше зомби в том же бюджете.

**Только для врагов.** Запечённая анимация лишает контроля над костями.
Зомби это переживут — они бегут и умирают. Бойцам не подойдёт: им
доворачивают грудь при прицеливании и вешают оружие на кость кисти.

---

## 7. Где мы сейчас

| | |
|---|---|
| кадр | 16.7 мс, 60 fps |
| GPU | 14.5–15.3 мс |
| **запас** | **около 1.5 мс** |
| сцена | 25 бойцов, ~20 зомби, 14 руин, тени |
| масштаб рендера | 1.0 |
| дальность теней | 60 м |
| HDR | включён (цены не имеет) |

Запас небольшой, и рост толпы его съест: каждый зомби стоит около
0.24 мс, из них примерно 0.1 мс — кости (см. §6). Первое, что стоит трогать при просадке, — не логика, а снова
площадь: сколько пикселей заливается и чем.

---

## 8. Что проверить, когда понадобится

- **Растущая толпа за один прогон.** Стенд умеет отключать группы, но не умеет
  наращивать нагрузку. Прогон, в котором зомби прибывают, а в логе видно,
  на какой численности кадр перестал укладываться, — следующий шаг.
- **Худшие кадры.** Средний кадр ровный, но отдельные доходят до 33 мс.
  Похоже на сборку мусора; стоит проверить выделения памяти в горячих циклах.
- **Материалы персонажей.** Они на Toony Colors Pro. По §6 материал зомби
  на кадре почти не сказался, так что срочности нет; но цифра «свой тун
  экономит 4 мс» снята при затёртом закреплении толпы и подлежит пересъёмке.
- **`APP_CMD_LOW_MEMORY`** в логе устройства. На телефоне 2.6 ГБ, и система
  уже предупреждает. Пока не смешивать с замерами кадра, но помнить.
